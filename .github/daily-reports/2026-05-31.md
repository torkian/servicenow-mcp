# Daily Report — 2026-05-31

## Task Completed
Added `bulk_update_change_requests` tool — PATCH multiple change requests in one Batch API call.

Implementation mirrors `bulk_update_incidents`: a `ChangeRequestUpdate` Pydantic model covers 13 updatable fields (short_description, description, state, type, category, risk, impact, priority, assignment_group, assigned_to, start_date, end_date, work_notes). Change request numbers (CHG…) are batch-resolved to sys_ids with a single preliminary GET before the batch PATCH is issued. Each result entry carries the original `change_id` for traceability. Registered in `change_coordinator` and `full` packages.

## Test Results
- Total tests: 1328
- Coverage: 91%
- All passing: yes

## Security
Pre-existing system-package advisories (pyjwt, setuptools, urllib3, wheel) — no new vulnerabilities introduced.

## Files Changed
- `src/servicenow_mcp/tools/bulk_tools.py` — added `ChangeRequestUpdate`, `BulkUpdateChangeRequestsParams`, `_resolve_change_request_numbers`, `bulk_update_change_requests`
- `src/servicenow_mcp/tools/__init__.py` — exported `bulk_update_change_requests`
- `src/servicenow_mcp/utils/tool_utils.py` — imported and registered `bulk_update_change_requests`
- `config/tool_packages.yaml` — added to `change_coordinator` and `full` packages
- `tests/test_bulk_update_change_requests.py` — 20 new tests

## Next Up
Add bulk_update_problems tool (PATCH multiple problems in one batch call)
