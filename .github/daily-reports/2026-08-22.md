# Daily Report — 2026-08-22

## Task Completed
Added `delete_assessment_instance` tool (DELETE asmt_assessment_instance/{sys_id}).

### Details
- New `DeleteAssessmentInstanceParams` Pydantic model with required `instance_id` (sys_id of the record to delete).
- `delete_assessment_instance` function issues DELETE against `asmt_assessment_instance/{instance_id}`.
- Returns 404-guarded error when no matching record exists.
- Handles both 204 No Content and 200 OK success responses.
- Registered in `__init__.py` (import + `__all__`), `tool_utils.py` (params import + tool registration with description), and `config/tool_packages.yaml` (system_administrator and full packages).

### Files Changed
- `src/servicenow_mcp/tools/assessment_tools.py` — added `DeleteAssessmentInstanceParams` model and `delete_assessment_instance` function
- `src/servicenow_mcp/tools/__init__.py` — added import and `__all__` entry
- `src/servicenow_mcp/utils/tool_utils.py` — added param/function imports and tool registration dict entry
- `config/tool_packages.yaml` — added to system_administrator and full packages
- `tests/test_assessment_tools.py` — 10 new tests covering all success/error/edge-case paths

## Test Results
- Total tests: 3139
- Coverage: 95%
- All passing: yes

## Security
Pre-existing transitive dependency advisories for pyjwt, setuptools, urllib3, wheel — not related to this change; no new vulnerabilities introduced.

## Next Up
Add list_catalog_item_questions tool (io_set_item and item_option_new; questions linked to a catalog item variable set)
