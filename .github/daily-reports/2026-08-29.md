# Daily Report — 2026-08-29

## Task Completed
Added `get_change_conflict` tool (GET change_conflict/{sys_id}).

- New `GetChangeConflictParams` Pydantic model with required `sys_id` field.
- `get_change_conflict()` function fetches a single record from the `change_conflict` table by sys_id.
- Returns normalised conflict detail via existing `_format_change_conflict()` helper: `change_request`, `conflict_ci`, `conflict_change`, `type`, `state`, `blackout_window`, `created_on`, `updated_on`.
- 404 guard (explicit status check before `raise_for_status`).
- Empty-result guard when the API returns `{}` or `None`.
- Sets `sysparm_display_value=all` and `sysparm_exclude_reference_link=true` for clean reference field output.
- Registered in `__init__.py`, `tool_utils.py`, `config/tool_packages.yaml` (change_coordinator + full packages).

## Test Results
- Total tests: 3259
- Coverage: 95%
- All passing: yes

## Security
No vulnerabilities detected (pip-audit clean; python-apt and servicenow-mcp skipped as local packages).

## Files Changed
- `src/servicenow_mcp/tools/change_tools.py` — added `GetChangeConflictParams` and `get_change_conflict`
- `src/servicenow_mcp/tools/__init__.py` — import + `__all__` entry
- `src/servicenow_mcp/utils/tool_utils.py` — import + registry entry
- `config/tool_packages.yaml` — added to `change_coordinator` and `full` packages
- `tests/test_get_change_conflict.py` — 14 new tests (success, 404, empty-result, connection error, missing URL/headers, invalid params)
- `.github/DAILY_BACKLOG.md` — moved item to Completed
- `.github/daily-reports/2026-08-29.md` — this file

## Next Up
Add list_incident_slas tool (task_sla scoped to incident table; filters for has_breached/stage; pagination; registered in service_desk + full)
