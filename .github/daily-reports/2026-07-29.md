# Daily Report — 2026-07-29

## Task Completed
Added Flow Designer flow tools (`list_flows`, `get_flow`) for the `sys_hub_flow` table.
Extends the workflow_activity_tools added yesterday to cover the actual Flow Designer
flows (not just action types).  `list_flows` supports filtering by name, active state,
publication status, category, and application scope.  `get_flow` resolves by sys_id or
exact name with 404/empty-result guards.  Both tools are registered in the
`platform_developer` and `full` packages.

## Test Results
- Total tests: 2509 (28 new)
- Coverage: 93%
- All passing: yes

## Security
Clean — no vulnerabilities found by pip-audit.

## Files Changed
- `src/servicenow_mcp/tools/flow_tools.py` (new)
- `tests/test_flow_tools.py` (new)
- `src/servicenow_mcp/tools/__init__.py`
- `src/servicenow_mcp/utils/tool_utils.py`
- `config/tool_packages.yaml`
- `.github/DAILY_BACKLOG.md`

## Next Up
Add `trigger_flow` tool (POST to Flow Designer execution API) to allow MCP clients to
execute flows on demand.
