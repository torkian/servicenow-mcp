# Daily Report — 2026-09-16

## Task Completed
Added `create_location`, `update_location`, and `delete_location` tools to complete
the full CRUD surface for the cmn_location table (following yesterday's list/get work).

- `create_location` — POST cmn_location; required name, 13 optional address/geo/ref fields
- `update_location` — PATCH by sys_id or exact name; empty-body guard; 404 guard
- `delete_location` — DELETE by sys_id or exact name; 404 guard on both resolve and delete steps
- All three registered in `__init__.py`, `tool_utils.py`, and `tool_packages.yaml`
  (system_administrator and full packages)

## Test Results
- Total tests: 3781
- Coverage: 95%
- All passing: yes

## Security
Pre-existing urllib3 (PYSEC-2026-141/142) and wheel (CVE-2026-24049) advisories
in the base environment; not introduced by today's change.

## Files Changed
- `src/servicenow_mcp/tools/location_tools.py` — added 3 param models + 3 tool functions
- `src/servicenow_mcp/tools/__init__.py` — added create/update/delete imports
- `src/servicenow_mcp/utils/tool_utils.py` — added imports + handler registrations
- `config/tool_packages.yaml` — added tools to system_administrator and full packages
- `tests/test_location_tools.py` — added 17 new tests (44 total for this module)

## Next Up
Add `list_departments` and `get_department` tools (cmn_department table)
