# Daily Report — 2026-08-20

## Task Completed
Added `create_assessment_instance` tool (POST `asmt_assessment_instance`).

- New `CreateAssessmentInstanceParams` model with required `metric_type`, `source_table`,
  `source_id` fields and optional `user`, `assigned_to`, `due_date` (validated YYYY-MM-DD),
  and `state`.
- `_resolve_user_sys_id` helper: passes through 32-char hex sys_ids; performs a
  `user_name=` lookup for plain strings; returns `None` on failure.
- Metric type resolved via existing `_resolve_metric_type_sys_id`; user and assigned_to
  resolved via `_resolve_user_sys_id`; all resolution failures surface human-readable errors.
- Body omits `None` optional fields; POST includes `sysparm_display_value=true` so returned
  reference fields come back as display values.
- Registered in `__init__.py`, `tool_utils.py` (with full description), and
  `config/tool_packages.yaml` (in both `system_administrator` and `full` packages).
- 18 new tests covering: helper passthrough / name-lookup / not-found / network-error paths,
  param validation (valid minimal, valid due_date, invalid due_date), no-instance_url /
  no-headers guards, metric_type / user / assigned_to resolution failures, minimal success,
  full-params success, sys_id passthrough (single HTTP call), HTTP error.

## Test Results
- Total tests: 3105
- Coverage: 95%
- All passing: yes

## Security
Pre-existing system package advisories (setuptools, urllib3, wheel) — not introduced by
this change.

## Files Changed
- `src/servicenow_mcp/tools/assessment_tools.py` — added `_resolve_user_sys_id`,
  `CreateAssessmentInstanceParams`, `create_assessment_instance`
- `src/servicenow_mcp/tools/__init__.py` — export `create_assessment_instance`
- `src/servicenow_mcp/utils/tool_utils.py` — import + tool definition
- `config/tool_packages.yaml` — added to `system_administrator` and `full` packages
- `tests/test_assessment_tools.py` — 18 new tests

## Next Up
Add `export_assessment_results` tool (aggregate scores and responses for a metric type;
optional date range).
